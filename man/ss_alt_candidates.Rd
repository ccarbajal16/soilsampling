% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alternative_sites.R
\name{ss_alt_candidates}
\alias{ss_alt_candidates}
\title{Generate Candidate Sites for Alternative Site Selection}
\usage{
ss_alt_candidates(
  raster_data,
  n_candidates,
  method = c("random", "systematic"),
  spacing = NULL,
  seed = NULL
)
}
\arguments{
\item{raster_data}{A \code{SpatRaster} stack of environmental covariates.}

\item{n_candidates}{Integer, number of candidate sites to generate.}

\item{method}{\code{"random"} (default) or \code{"systematic"} (regular grid).}

\item{spacing}{Numeric, grid spacing for \code{method = "systematic"} (in
raster coordinate units). If \code{NULL} (default), calculated from the
raster extent to approximate \code{n_candidates}.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A data frame with \code{site_id}, \code{x}, \code{y}, \code{type} (\code{"candidate"}),
and one column per raster layer.
}
\description{
Generates a pool of candidate sites (random or on a systematic grid)
within a raster's extent, restricted to valid (non-\code{NA}) cells, and
extracts their environmental values.
}
\examples{
\dontrun{
r <- terra::rast("data/predictors.tif")
candidates <- ss_alt_candidates(r, n_candidates = 1000, seed = 123)
}

}
\seealso{
\code{\link[=ss_alt_similarity]{ss_alt_similarity()}}, \code{\link[=ss_alt_filter_buffer]{ss_alt_filter_buffer()}}, \code{\link[=ss_alt_sites]{ss_alt_sites()}}
}
