% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alternative_sites.R
\name{ss_alt_rank}
\alias{ss_alt_rank}
\title{Rank and Select the Top Similar Alternative Sites}
\usage{
ss_alt_rank(
  similarity_scores,
  candidate_sites,
  n_select = 5,
  target_site_id = NULL
)
}
\arguments{
\item{similarity_scores}{Numeric vector of similarity scores, as
returned by \code{\link[=ss_alt_similarity]{ss_alt_similarity()}}, one per row of \code{candidate_sites}.}

\item{candidate_sites}{Data frame of candidate sites, as returned by
\code{\link[=ss_alt_candidates]{ss_alt_candidates()}} or \code{\link[=ss_alt_filter_buffer]{ss_alt_filter_buffer()}}.}

\item{n_select}{Integer, number of top alternatives to select. If
fewer valid (non-\code{NA}, finite) scores are available, all of them are
returned with a warning.}

\item{target_site_id}{Optional character, ID of the target site these
alternatives are for; stored in a \code{target_site_id} column. If \code{NULL}
(default), that column is filled with \code{NA}.}
}
\value{
A data frame of the top \code{n_select} candidate sites, with added
\code{target_site_id}, \code{similarity_score}, \code{similarity_rank}, and
\code{selection_method} columns. Empty data frame if no valid scores.
}
\description{
Orders candidate sites by similarity score (highest first) and returns
the top \code{n_select}, annotated with rank and target site information.
}
\examples{
candidates <- data.frame(site_id = paste0("c", 1:5), x = 1:5, y = 1:5)
scores <- c(0.9, 0.4, 0.95, 0.2, 0.7)
ss_alt_rank(scores, candidates, n_select = 2, target_site_id = "site_001")

}
\seealso{
\code{\link[=ss_alt_similarity]{ss_alt_similarity()}}, \code{\link[=ss_alt_sites]{ss_alt_sites()}}
}
