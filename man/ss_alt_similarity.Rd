% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alternative_sites.R
\name{ss_alt_similarity}
\alias{ss_alt_similarity}
\title{Environmental Similarity Between a Target Site and Candidate Sites}
\usage{
ss_alt_similarity(
  target_values,
  candidate_values,
  categorical_vars = NULL,
  weights = NULL,
  method = c("mahalanobis", "euclidean", "gower"),
  normalize = TRUE,
  missing_method = "pairwise"
)
}
\arguments{
\item{target_values}{Environmental values at the target site: a named
numeric vector, or a single-row data frame with the same columns as
\code{candidate_values}.}

\item{candidate_values}{Environmental values at candidate sites: a
matrix or data frame, one row per candidate.}

\item{categorical_vars}{Character vector of categorical column names.
Only used by \code{method = "gower"}, and excluded from the other methods.}

\item{weights}{Optional named numeric vector of variable importance
weights; unnamed weights must match \code{length(target_values)}.}

\item{method}{Distance metric: \code{"mahalanobis"} (default, accounts for
covariance among variables), \code{"euclidean"}, or \code{"gower"} (handles
mixed continuous/categorical data).}

\item{normalize}{Logical, whether to z-score normalize numeric
variables before distance calculation. Default \code{TRUE}.}

\item{missing_method}{How to handle missing values: \code{"pairwise"}
(default, handled per-comparison), \code{"listwise"}, \code{"mean"}, or
\code{"median"}.}
}
\value{
Numeric vector of similarity scores in \verb{[0, 1]}, one per
candidate row (\code{1} = perfect match).
}
\description{
Computes a 0-1 similarity score between a target (e.g. an inaccessible
sampling site) and a set of candidate sites, based on multivariate
distance in environmental covariate space.
}
\examples{
target <- c(dem = 4000, slope = 0.3, ndvi = 0.4)
candidates <- data.frame(
  dem = c(3990, 4200, 3500),
  slope = c(0.31, 0.5, 0.1),
  ndvi = c(0.41, 0.2, 0.6)
)
ss_alt_similarity(target, candidates)

}
\seealso{
\code{\link[=ss_alt_candidates]{ss_alt_candidates()}}, \code{\link[=ss_alt_rank]{ss_alt_rank()}}, \code{\link[=ss_alt_sites]{ss_alt_sites()}}
}
