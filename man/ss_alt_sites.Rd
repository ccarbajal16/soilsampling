% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alternative_sites.R
\name{ss_alt_sites}
\alias{ss_alt_sites}
\title{Find Alternative Sampling Sites for Inaccessible Locations}
\usage{
ss_alt_sites(
  covariates,
  target_sites,
  method = c("mahalanobis", "euclidean", "gower"),
  n_alternatives = 5,
  n_candidates = NULL,
  min_distance_buffer = NULL,
  categorical_vars = NULL,
  weights = NULL,
  normalize = TRUE,
  missing_method = "pairwise",
  candidate_method = c("random", "systematic"),
  seed = NULL,
  output_dir = NULL
)
}
\arguments{
\item{covariates}{A character path to a \code{.tif} file or directory (see
\code{\link[=ss_load_rasters]{ss_load_rasters()}}), or an already-loaded \code{SpatRaster} stack.}

\item{target_sites}{Data frame of sites needing alternatives, with \code{x}
and \code{y} coordinate columns. A \code{site_id} column is generated if
missing. Environmental values are extracted automatically for any
covariate layer not already present as a column.}

\item{method}{Similarity metric: \code{"mahalanobis"} (default),
\code{"euclidean"}, or \code{"gower"}. Passed to \code{\link[=ss_alt_similarity]{ss_alt_similarity()}}.}

\item{n_alternatives}{Integer, number of alternatives per target site.
Default \code{5}.}

\item{n_candidates}{Integer, size of the candidate pool. If \code{NULL}
(default), set to \code{max(n_alternatives * 50, 1000)}.}

\item{min_distance_buffer}{Numeric, minimum distance (raster
coordinate units) candidates must keep from every target site. If
\code{NULL} (default), no buffer is applied.}

\item{categorical_vars}{Character vector of categorical covariate
names. Passed to \code{\link[=ss_alt_similarity]{ss_alt_similarity()}}.}

\item{weights}{Optional named numeric vector of variable weights.
Passed to \code{\link[=ss_alt_similarity]{ss_alt_similarity()}}.}

\item{normalize}{Logical, passed to \code{\link[=ss_alt_similarity]{ss_alt_similarity()}}. Default \code{TRUE}.}

\item{missing_method}{Passed to \code{\link[=ss_alt_similarity]{ss_alt_similarity()}}. Default \code{"pairwise"}.}

\item{candidate_method}{\code{"random"} (default) or \code{"systematic"}.
Passed to \code{\link[=ss_alt_candidates]{ss_alt_candidates()}}.}

\item{seed}{Optional integer seed for reproducibility.}

\item{output_dir}{Character, directory to write CSV outputs to. If
\code{NULL} (default), nothing is written to disk.}
}
\value{
A list with:
\describe{
\item{target_sites}{Data frame, targets with environmental values.}
\item{candidate_sites}{Data frame, the (buffer-filtered) candidate pool.}
\item{alternatives}{Data frame, all selected alternatives for all
target sites, with a \code{target_site_id} column.}
\item{selected_by_site}{Named list of per-target alternative data
frames, keyed by \code{site_id}.}
\item{method_info}{List recording the parameters used.}
\item{file_paths}{Only when \code{output_dir} is supplied: named list
of files written.}
}
}
\description{
End-to-end workflow: generates a candidate pool (\code{\link[=ss_alt_candidates]{ss_alt_candidates()}}),
optionally excludes candidates near the target sites
(\code{\link[=ss_alt_filter_buffer]{ss_alt_filter_buffer()}}), then computes environmental similarity
(\code{\link[=ss_alt_similarity]{ss_alt_similarity()}}) and selects the top alternatives
(\code{\link[=ss_alt_rank]{ss_alt_rank()}}) for each target (e.g. inaccessible) site.
}
\examples{
\dontrun{
inaccessible <- read.csv("data/inaccessible_sites.csv")
res <- ss_alt_sites("data/predictors.tif", inaccessible,
  n_alternatives = 3, min_distance_buffer = 300, seed = 123
)
res$alternatives
}

}
\seealso{
\code{\link[=ss_alt_candidates]{ss_alt_candidates()}}, \code{\link[=ss_alt_filter_buffer]{ss_alt_filter_buffer()}},
\code{\link[=ss_alt_similarity]{ss_alt_similarity()}}, \code{\link[=ss_alt_rank]{ss_alt_rank()}}
}
