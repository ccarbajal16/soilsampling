% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alternative_sites.R
\name{ss_alt_standardize_sites}
\alias{ss_alt_standardize_sites}
\title{Standardize Site Coordinate Data}
\usage{
ss_alt_standardize_sites(
  sites_data,
  x_col = "x",
  y_col = "y",
  id_col = NULL,
  site_type = "inaccessible"
)
}
\arguments{
\item{sites_data}{Data frame with at least x/y coordinate columns.}

\item{x_col}{Character, name of the x coordinate column. Default \code{"x"}.}

\item{y_col}{Character, name of the y coordinate column. Default \code{"y"}.}

\item{id_col}{Optional character, name of an existing site ID column.
If \code{NULL} or not found, IDs are generated as \code{"<site_type>_<n>"}.}

\item{site_type}{Character, value for the \code{type} column and the ID
prefix when \code{id_col} is not supplied. Default \code{"inaccessible"}.}
}
\value{
A data frame with \code{site_id}, \code{x}, \code{y}, \code{type} as the first
four columns, followed by any remaining columns from \code{sites_data}.
}
\description{
Normalizes a data frame of site coordinates (typically loaded from
CSV) to the \code{site_id}, \code{x}, \code{y}, \code{type} column layout used throughout
the \verb{ss_alt_*} functions.
}
\examples{
raw <- data.frame(lon = c(629500, 630200), lat = c(9879500, 9880100))
ss_alt_standardize_sites(raw, x_col = "lon", y_col = "lat")

}
\seealso{
\code{\link[=ss_alt_sites]{ss_alt_sites()}}
}
