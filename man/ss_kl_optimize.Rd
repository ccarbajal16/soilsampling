% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kl_optimization.R
\name{ss_kl_optimize}
\alias{ss_kl_optimize}
\title{Optimize cLHS Sample Size Using KL Divergence}
\usage{
ss_kl_optimize(
  population_data,
  min_samples = 10,
  max_samples = 500,
  step_size = 10,
  n_replicates = 10,
  n_bins = 25,
  probability_threshold = 0.95
)
}
\arguments{
\item{population_data}{Data frame of population ancillary data (one row
per population unit, one column per covariate).}

\item{min_samples}{Integer, minimum sample size to test. Default \code{10}.}

\item{max_samples}{Integer, maximum sample size to test. Default \code{500}.}

\item{step_size}{Integer, increment between tested sample sizes.
Default \code{10}.}

\item{n_replicates}{Integer, number of cLHS replicates per sample size.
Default \code{10}.}

\item{n_bins}{Integer, number of histogram bins used by
\code{\link[=ss_kl_divergence]{ss_kl_divergence()}}. Default \code{25}.}

\item{probability_threshold}{Numeric in (0, 1], CDF threshold used to
pick the optimal sample size. Default \code{0.95}.}
}
\value{
A list with:
\describe{
\item{raw_results}{Data frame, one row per replicate.}
\item{summary_results}{Data frame, mean/sd KL divergence per sample size.}
\item{fitted_model}{The fitted \code{nls} exponential decay model, or \code{NULL}.}
\item{fitted_curve}{Data frame of fitted KL divergence per sample size.}
\item{optimal_sample_size}{Integer, the recommended sample size.}
\item{plot_kl}{A \code{ggplot} of KL divergence vs. sample size.}
\item{plot_cdf}{A \code{ggplot} of the CDF used to pick the optimal size.}
}
}
\description{
Determines the sample size at which a conditioned Latin hypercube sample
(cLHS) best represents the population distribution, following the
KL-divergence-based approach of Malone et al. (2019). Runs cLHS at a
range of sample sizes, fits an exponential decay curve to the mean KL
divergence, and reports the smallest sample size that reaches a target
proportion of the maximum achievable improvement.
}
\details{
The relationship between sample size \eqn{n} and KL divergence is modeled
as \eqn{KL(n) = b_1 e^{-kn} + b_0}. The optimal sample size is the
smallest \eqn{n} for which the cumulative proportion of improvement,
\eqn{(max(KL) - KL(n)) / (max(KL) - min(KL))}, reaches
\code{probability_threshold}.
}
\examples{
\dontrun{
pop <- data.frame(a = rnorm(2000), b = runif(2000))
res <- ss_kl_optimize(pop, min_samples = 10, max_samples = 60,
  step_size = 10, n_replicates = 3)
res$optimal_sample_size
}

}
\seealso{
\code{\link[=ss_kl_divergence]{ss_kl_divergence()}}
}
