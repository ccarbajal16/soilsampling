% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kl_optimization.R
\name{ss_kl_size}
\alias{ss_kl_size}
\title{End-to-End cLHS Sample Size Optimization}
\usage{
ss_kl_size(
  x,
  output_dir = NULL,
  max_population = 1e+05,
  min_samples = 10,
  max_samples = 500,
  step_size = 10,
  n_replicates = 10,
  n_bins = 25,
  probability_threshold = 0.95
)
}
\arguments{
\item{x}{One of: a character path to a \code{.tif} file or a directory of
\code{.tif} files, a \code{SpatRaster} stack, or a data frame of population
ancillary data (used as-is, skipping raster extraction).}

\item{output_dir}{Character, directory to write CSV/PNG outputs to. If
\code{NULL} (default), nothing is written to disk.}

\item{max_population}{Integer, if the population has more rows than this,
a random subsample of this size is used to keep runtime reasonable.
Default \code{100000}.}

\item{min_samples, max_samples, step_size, n_replicates, n_bins, probability_threshold}{Passed to \code{\link[=ss_kl_optimize]{ss_kl_optimize()}}.}
}
\value{
The list returned by \code{\link[=ss_kl_optimize]{ss_kl_optimize()}}, plus a \code{file_paths}
element (only when \code{output_dir} is supplied) listing the files written.
}
\description{
Runs \code{\link[=ss_kl_optimize]{ss_kl_optimize()}} starting from predictor rasters (a file path, a
directory of \code{.tif} files, or an already-loaded \code{SpatRaster}) or directly
from a population data frame. Optionally writes results and plots to
disk.
}
\examples{
\dontrun{
res <- ss_kl_size("data/predictors.tif", output_dir = "outputs")
res$optimal_sample_size
}

}
\seealso{
\code{\link[=ss_kl_optimize]{ss_kl_optimize()}}, \code{\link[=ss_load_rasters]{ss_load_rasters()}}
}
