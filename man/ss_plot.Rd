% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{ss_plot}
\alias{ss_plot}
\title{Plot Stratification}
\usage{
ss_plot(
  x,
  samples = NULL,
  show_boundaries = TRUE,
  show_centroids = FALSE,
  fill_color = "#80CC80",
  boundary_color = "#CCE5CC",
  sample_color = "black",
  sample_alpha = 0.7,
  sample_size = 2,
  ...
)
}
\arguments{
\item{x}{An object of class \code{ss_strata} or \code{ss_samples}.}

\item{samples}{Optional \code{ss_samples} object to overlay on stratification.}

\item{show_boundaries}{Logical, whether to show stratum boundaries (default TRUE).}

\item{show_centroids}{Logical, whether to show stratum centroids (default FALSE).}

\item{fill_color}{Character, fill color for the study area (default "#80CC80").}

\item{boundary_color}{Character, color for stratum boundaries (default "#CCE5CC").}

\item{sample_color}{Character, color for sampling points (default "black").}

\item{sample_alpha}{Numeric, transparency for sampling points (default 0.7).}

\item{sample_size}{Numeric, size of sampling points (default 2).}

\item{...}{Additional arguments passed to ggplot2 functions.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a visualization of the spatial stratification.
}
\examples{
\dontrun{
library(sf)
poly <- st_polygon(list(rbind(c(0, 0), c(100, 0), c(100, 50), c(0, 50), c(0, 0))))
study_area <- st_sf(geometry = st_sfc(poly))

strata <- ss_stratify(study_area, n_strata = 25)
ss_plot(strata)

samples <- ss_coverage(strata)
ss_plot(strata, samples = samples)
}

}
\seealso{
\code{\link[=ss_plot_samples]{ss_plot_samples()}}
}
