% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rf_optimization.R
\name{ss_rf_mse}
\alias{ss_rf_mse}
\title{Cross-Validated Random Forest MSE for a Sample Design}
\usage{
ss_rf_mse(sample_points, full_data = NULL, n_folds = 5, target_var = NULL)
}
\arguments{
\item{sample_points}{Data frame with \code{x}, \code{y} as the first two columns
followed by covariate columns (as returned by \code{\link[=ss_clhs_sample]{ss_clhs_sample()}}).}

\item{full_data}{Optional data frame of the full covariate population;
if supplied, \code{sample_points} is restricted to the shared columns.}

\item{n_folds}{Integer, number of cross-validation folds. Default \code{5}.}

\item{target_var}{Character, name of the covariate to predict. If
\code{NULL} (default), the third covariate column is used, or the first
if fewer than three are available.}
}
\value{
Numeric, the mean cross-validated MSE, or \code{NA_real_} if the
design has too few rows/columns or every fold fails.
}
\description{
Trains a Random Forest with k-fold cross-validation on a candidate
sample design and returns the mean squared error for a chosen response
covariate. Used as the objective function for \code{\link[=ss_rf_optimize]{ss_rf_optimize()}}.
}
\examples{
\dontrun{
samples <- ss_clhs_sample(r, n_samples = 100, seed = 1)
ss_rf_mse(samples)
}

}
\seealso{
\code{\link[=ss_rf_optimize]{ss_rf_optimize()}}
}
