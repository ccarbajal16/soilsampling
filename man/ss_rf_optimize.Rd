% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rf_optimization.R
\name{ss_rf_optimize}
\alias{ss_rf_optimize}
\title{Optimize a Sample Design With Random Forest Simulated Annealing}
\usage{
ss_rf_optimize(
  covariates,
  n_samples,
  n_iterations = 500,
  seed = NULL,
  temperature = 1000,
  cooling_rate = 0.95,
  target_var = NULL
)
}
\arguments{
\item{covariates}{A \code{SpatRaster} stack of environmental covariates.}

\item{n_samples}{Integer, number of samples in the design.}

\item{n_iterations}{Integer, number of simulated annealing iterations.
Default \code{500}.}

\item{seed}{Optional integer seed. The cLHS baseline uses \code{seed}; the
annealing loop uses \code{seed + 1}, so the two stages stay reproducible
but independent.}

\item{temperature}{Numeric, initial annealing temperature. Default \code{1000}.}

\item{cooling_rate}{Numeric in (0, 1), multiplicative cooling factor
applied each iteration. Default \code{0.95}.}

\item{target_var}{Character, covariate to use as the RF response.
Passed to \code{\link[=ss_rf_mse]{ss_rf_mse()}}.}
}
\value{
A list with:
\describe{
\item{optimized_samples}{Data frame, the best design found.}
\item{initial_samples}{Data frame, the cLHS baseline design.}
\item{initial_mse}{Numeric, baseline MSE.}
\item{final_mse}{Numeric, MSE of \code{optimized_samples}.}
\item{improvement}{Numeric, percent MSE reduction vs. baseline.}
}
}
\description{
Starts from a cLHS baseline and iteratively swaps sample points using
simulated annealing to minimize cross-validated Random Forest MSE
(\code{\link[=ss_rf_mse]{ss_rf_mse()}}), following Wadoux et al. (2019).
}
\examples{
\dontrun{
r <- terra::rast("data/predictors.tif")
res <- ss_rf_optimize(r, n_samples = 100, n_iterations = 500, seed = 123)
res$improvement
}

}
\seealso{
\code{\link[=ss_clhs_sample]{ss_clhs_sample()}}, \code{\link[=ss_rf_mse]{ss_rf_mse()}}, \code{\link[=ss_rf_size]{ss_rf_size()}}
}
