# Normalize Feature Matrix

Normalizes features to have zero mean and unit variance.

## Usage

``` r
.normalize_features(X)
```

## Arguments

- X:

  Numeric matrix

## Value

List with normalized matrix and scaling parameters
