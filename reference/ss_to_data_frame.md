# Convert to Data Frame

Convert to Data Frame

## Usage

``` r
ss_to_data_frame(x, what = "samples")
```

## Arguments

- x:

  An object of class `ss_samples` or `ss_strata`

- what:

  Character, what to extract (default "samples")

## Value

A data frame with coordinates
