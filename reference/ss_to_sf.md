# Convert to sf Object

Convert to sf Object

## Usage

``` r
ss_to_sf(x, what = "samples")
```

## Arguments

- x:

  An object of class `ss_samples` or `ss_strata`

- what:

  Character, what to extract: "samples", "cells", or "centroids"

## Value

An `sf` object
